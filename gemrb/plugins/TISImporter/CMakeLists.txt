IF (JPEG_FOUND)
	SET(TIZ_SOURCE TIZImporter.cpp)
ENDIF (JPEG_FOUND)

ADD_GEMRB_PLUGIN (TISImporter TISImporter.cpp TISTile.cpp ${TIZ_SOURCE})

IF (JPEG_FOUND)
	SET_TARGET_PROPERTIES(TISImporter PROPERTIES
		COMPILE_DEFINITIONS
		HAS_JPEG_SUPPORT=1
	)
ENDIF (JPEG_FOUND)
